[package]
name = "chess"
description = "A chess game implemented using iced for the GUI."
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
iced = { version = "0.13.1", features = ["svg"] }
